# missing-person-information
A simple web application to manage missing person data. Users can register, login, and perform CRUD operations (Create, Read, Update, Delete) on missing person records. Built with Streamlit for the front-end and SQLite for database management.
